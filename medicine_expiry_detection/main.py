from camera_module import process_camera_image #handles camera capture and processing

if __name__ == "__main__": #Checks if this script is being run directly (not imported). 
    process_camera_image()
